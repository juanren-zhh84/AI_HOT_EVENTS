# 计划.md 整理方案

## Summary
在 `doc/计划.md` 新增一份开发计划文档，把当前项目从“基础服务已连库”推进到“GitHub 采集、快照、热点计算、邮件日报”的实施路径整理清楚。文档使用 UTF-8 Markdown，和现有 `需求文档.md`、`接口文档.md`、`数据库建表语句.sql` 放在同一目录。

## Key Changes
- 新增 `doc/计划.md`。
- 文档结构固定为：
  - 当前状态
  - 开发阶段总览
  - 下一步优先任务
  - 每阶段交付物
  - 验收标准
  - 注意事项
- 计划内容采用以下顺序：
  1. 项目卫生：确保 `.env` 不提交，保留 `.env.example` 思路。
  2. ORM 模型：先实现 `repositories`、`star_snapshots`、`jobs`。
  3. GitHubClient：封装仓库详情、languages、README、rate_limit。
  4. 仓库接口：新增、列表、详情、启停监控。
  5. 星标快照：手动触发采集并写入 `star_snapshots`。
  6. 热点计算：计算 24h/7d 增长并写入 `hot_projects`。
  7. 邮件日报：最后实现模板、订阅者、SMTP 发送和定时任务。

## Test Plan
- 确认 `doc/计划.md` 文件存在。
- 确认 Markdown 中文正常显示，无乱码。
- 确认文档不包含真实 `.env` 密钥、GitHub Token、SMTP 密码。
- 确认计划与当前代码状态一致：当前已有 FastAPI 入口、配置读取、MySQL health check、requirements。
- 确认计划与 `需求文档.md`、`接口文档.md`、`数据库建表语句.sql` 不冲突。

## Assumptions
- 计划文件路径使用 `doc/计划.md`。
- 本地开发环境使用用户自己的 Conda 环境。
- 服务器部署时再使用 `.venv`。
- 下一阶段优先做仓库采集闭环，不先做邮件和调度。

## 当前进度更新

截至当前阶段，前 1-7 步已经基本完成：

1. 项目卫生：已建立 `.env` 配置思路，并确认本地开发使用 Conda、服务器部署使用 `.venv`。
2. ORM 模型：已实现仓库、星标快照、热点项目、订阅者、邮件日报、投递记录等核心模型。
3. GitHubClient：已封装 GitHub 仓库信息、语言占比、限流信息等基础能力。
4. 仓库接口：已实现仓库新增、列表、详情、启停监控等接口。
5. 星标快照：已实现手动触发采集，并能写入 `star_snapshots`。
6. 热点计算：已实现热点项目计算，并能写入 `hot_projects`。
7. 邮件日报：已实现订阅者、日报生成、SMTP 发送，并修复了同一天重复发送日报导致 `email_deliveries` 唯一约束冲突的问题。

因此，下一阶段不再继续补 1-7 步的业务主链路，而是进入“自动化运行”和“部署准备”阶段。

## 后续计划

后续 8-10 阶段已拆分到独立文档：[后续阶段计划.md](后续阶段计划.md)。

主计划文档只保留当前阶段总结，后续自动调度、自动化测试和部署准备的详细内容统一维护在独立文档中。
